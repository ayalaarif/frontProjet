import { GroupeEnseignants } from './groupe-enseignants.model';

describe('GroupeEnseignants', () => {
  it('should create an instance', () => {
    expect(new GroupeEnseignants()).toBeTruthy();
  });
});
